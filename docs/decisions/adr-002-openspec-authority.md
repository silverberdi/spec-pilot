# ADR-002 — OpenSpec remains authoritative

**Status:** Accepted

SpecPilot reads and evaluates OpenSpec state but never invents a parallel lifecycle. Slash commands remain executor-facing; SpecPilot uses deterministic repository inspection and official CLI outputs where available.
