# ADR-005 — Portable project contract plus centralized state

**Status:** Accepted

`.specpilot/project.yaml` is required and portable. PostgreSQL stores normalized immutable snapshots and operational records. Local absolute paths remain installation-specific database data.
