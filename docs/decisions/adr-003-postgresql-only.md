# ADR-003 — PostgreSQL operational persistence

**Status:** Accepted

SQLite is excluded. PostgreSQL stores all registered projects, configuration snapshots, review runs, findings, prompts, usage, costs, and audit state.
