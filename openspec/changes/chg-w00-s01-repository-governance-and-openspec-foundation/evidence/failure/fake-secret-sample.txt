# Failure-path: fake secret sample

Timestamp: 2026-07-24T07:29:21Z

## Fixture

Created disposable file `tmp-secret-scan-fixture.txt` containing a synthetic
credential-like assignment matching the scanner's generic API-key pattern, then
deleted the fixture. The live secret string is intentionally NOT retained here
so committed evidence does not re-trigger fail-closed scanning.

## Command

```bash
python3 scripts/scan-secrets.py
```

## Outcome

- Exit code: `1` (non-zero as required)
- Explicit FAIL for `tmp-secret-scan-fixture.txt` with finding type
  `generic_api_key_assignment`
- Final stderr summary: `FAIL secret-scan (1 finding(s))`

Fixture removed after capture (see `restore-confirmation.txt`).
