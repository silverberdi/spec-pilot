# First Change Brief

Change: `chg-w00-s01-repository-governance-and-openspec-foundation`

Purpose: formally adopt and verify repository governance, the OpenSpec lifecycle, Cursor operating rules, canonical project context, lowercase identifier rules, generated-integration integrity, and deterministic baseline validation before technical scaffolding.

Current roles:
- Cursor: only implementer.
- Cline with DeepSeek: optional read-only validation assistant.
- Codex and OpenCode: no current project role.

Must include:
- canonical delivery hierarchy;
- explicit, approved repository branch/PR policy or an explicit statement that no special model is yet binding;
- exact Verify `PASS` rule and no `PASS WITH NOTES` closure;
- deviation synchronization;
- Cursor-only implementation contract;
- optional Cline read-only validation boundary;
- immutable OpenSpec-generated integrations;
- context index/generation/integrity mechanism;
- safe baseline validation scripts.

Must exclude Nx, Angular, PrimeNG, NestJS, PostgreSQL, Prisma, Docker, DeepSeek product integration, authentication, and all later-slice functionality.
